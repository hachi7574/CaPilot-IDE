# Test Report — 2026-08-17 03:56 (+08)

agentId: `d62ccd50-e114-4625-a058-6bb75a76dad6`  
role: Test  
cwd: `/home/hachi/.paseo/worktrees/293djwjk/precious-husky`  
branch: `analyze/acp-agent-runtime-vs-current`

## 0. 实际改动（本轮审查对象）

| 来源 | 结论 |
| --- | --- |
| `git log` | 无 ACP 相关 commit；HEAD 仍为 `4e7523286 feat: v0.1.4…` |
| `git status` | 无业务实现 diff；仅 `LeftSidebar.tsx` 用户改动（禁动）、`Cargo.lock` 因 `pnpm`/本地 cargo 可能脏、`docs/acp-*` untracked |
| Dev 产物 | `docs/acp-dev-log/20260817-0350-phase0-opencode-raw.json`、`…0355-phase0-opencode-extended.json`（摸底中，**未**见 `phase_gate=dev_done`、**未**回写附录 A.2） |
| 产品代码 | **无** `src-tauri/src/agent_runtime/acp/`；**无** `AcpBridge` / `acp_prompt` / `isAcpRuntime` |

**判定：** 无 Phase 实现可验收交付。本轮不做「假 pass」。改为 **Phase 0 基线 + 锚点对抗摸底 + 回归基线**，并把设计与本机 OpenCode 冲突升为 defect，供 Dev 在附录 A / Host 设计落地时处理。

---

## 1. 本轮测试计划（自拟，非文档复读）

角度（按「当前实际状态」裁剪）：

1. **基线库存** — 是否已有 ACP 分叉；`get_adapter` 对未知 id 的行为（`acp:*` 风险）
2. **回归** — 全量 `cargo test`（PTY 无损基线）
3. **前端合约基线** — `tsc`；现有 `runtime === "opencode"` / `agent_write` 分布（未来泄漏面地图）
4. **锚点协议** — 真实 `opencode acp` stdio：initialize / session/new / set_config / prompt / load / list / cancel 族 / close / 未知方法
5. **鲁棒** — 坏 method、rate-limit 错误形态、进程在 session/close 后是否仍存活
6. **对抗（设计 vs 实测）** — 设计稿写死的 `session/cancel` 是否在锚点上存在

不适用本 diff：§12 UI 项、permission 卡、fs 沙箱实现、Settings CRUD（无代码）。

---

## 2. 命令与结果

### 2.1 库存 / 静态

```text
ls src-tauri/src/agent_runtime/     → 无 acp/
rg is_acp|AcpBridge|acp_prompt      → 无命中
get_adapter match _ => ClaudeAdapter::new()   # runtimes/mod.rs:20
lib.rs 多处 get_adapter(runtime) 无 is_acp 前置分叉
```

**FAIL（预埋风险，记 DEF-001）：** 任何未来的 `runtime="acp:opencode"` 若先走到 `get_adapter`，会变成 **Claude PTY adapter**。

### 2.2 回归 `cargo test`

```bash
cd src-tauri && cargo test
# 206 passed; 0 failed; 1 ignored (usage::fetch_opencode_end_to_end manual)
# + tests/daemon_smoke.rs 3 passed
# finished ~3.7s
```

**PASS** — PTY 基线绿（本轮无 ACP 代码可破坏它）。

### 2.3 前端 `tsc`

```bash
pnpm install          # 本 worktree 缺 node_modules，已安装
pnpm exec tsc --noEmit
# exit 0，无输出
```

**PASS**（基线）。  
泄漏面地图（**当前正确**，因尚无 ACP；Phase 2 必回归）：

- `Composer.tsx`：大量 `agent_write` + `agent.runtime === "opencode"`（PTY 方言：F12/permissions、model 键序列等）
- `ContentArea.tsx:248`：`runtime === "opencode"` 用于 `openCodeTabs`
- `agentActions.ts`：发送路径一律 `agent_write`（尚无 `acp_prompt` 分发）

### 2.4 锚点 `opencode acp`（1.18.18）

原始 NDJSON：`docs/acp-dev-log/20260817-0355-test-opencode-acp-raw.ndjson`  
（Dev 并行日志：`…0350-phase0-opencode-raw.json`、`…0355-phase0-opencode-extended.json` — 与 Test 结论一致方向）

| 步骤 | 结果 | 证据摘要 |
| --- | --- | --- |
| launch | OK | `opencode acp` help：start ACP server；`--cwd` 可用 |
| initialize `protocolVersion:1` | OK | `agentInfo={OpenCode,1.18.18}`；`authMethods=[{id:opencode-login}]` |
| agentCapabilities | OK | `loadSession:true`；`promptCapabilities.image/embeddedContext`；`sessionCapabilities:{close,fork,list,resume}`；mcp http/sse |
| session/new | OK | `sessionId` 形如 `ses_…`；附带大量 `configOptions`（model select 等） |
| session/set_config_option | OK | `{sessionId, configId:"model", value:"opencode/deepseek-v4-flash-free"}` → `currentValue` 已切换 |
| session/prompt | **部分** | 协议接受；本机 provider **Rate limit** → JSON-RPC `-32603` `APIError`；未见 `agent_message_chunk` 正文 |
| session/update 已见表 | OK | `available_commands_update`、`usage_update{used,size,cost}`；load 时可见 `user_message_chunk` |
| session/cancel | **不存在** | `-32601 Method not found: session/cancel` |
| cancel/abort/stop/interrupt/`$/cancelRequest` 等 | **均不存在** | 同 -32601 |
| session/close | OK | `result:{}`；**进程仍存活**，可再 `session/new` |
| session/load | OK | 同 sid + cwd + mcpServers → result（含 configOptions）；并回放类 update |
| session/list | OK | `sessions:[{sessionId,cwd,title,updatedAt},…]` |
| session/resume | 参数需 cwd | 缺 cwd → `-32602 Invalid params`（未做完整成功路径） |
| authenticate `opencode-login` | 返回 `{}` | 未验证是否真登录；描述要求终端 `opencode auth login` |
| 未知 method | OK | `-32601`，会话未崩 |

**锚点结论：**

- 握手 + session 生命周期（new/load/list/close）**可用**，足以支撑 Phase 1 Host 对接。
- **完整 prompt 流式正文**本轮被 **OpenCode Console rate limit** 挡住（非协议分帧问题）。mock 绿仍 **不能** 代替锚点 prompt 过门。
- **设计稿 `session/cancel` 与 OpenCode 1.18.18 不符（DEF-002）** — DoD「cancel」不能照抄方法名；需 capability 探测或映射到 `session/close` / kill 子进程 / 其它官方方法（若后续版本补齐）。

---

## 3. Defects 裁定

| id | sev | title | 是否 blocker Phase0 出口 |
| --- | --- | --- | --- |
| DEF-001 | major | `get_adapter` 默认 Claude；无 `is_acp_runtime` 前置 | Phase1 合入前必须修；现无 acp 代码故未炸 |
| DEF-002 | **blocker（相对设计/ DoD 字面 cancel）** | OpenCode 无 `session/cancel` | Phase0 附录必须记录；Host 禁止写死 cancel RPC |
| DEF-003 | major | 附录 A.2 / phase checklist 未闭合；无成功 prompt 正文日志 | Phase0 出口未满足 |
| DEF-004 | minor | 默认 model `opencode/big-pickle` 易触 rate limit；应用 `set_config_option` 切 free/flash | 实现提示 |

---

## 4. phase_gate 结论

```text
phase_gate = test_failed   # 非「实现测挂」，而是：无可验收 dev_done + Phase0 出口未闭合 + DEF-002
current_phase = 0
```

**不**勾选 DoD / §12（无实现）。  
**不**把 cargo/tsc 绿解释成 ACP 进度。

---

## 5. 给 Dev 的独立要求（非复读其 handoff）

1. 完成 Phase0：**回写** `docs/acp-runtime-plan.md` 附录 A.2（capabilities 原文、cancel=N/A 或替代、loadSession 参数、set_config_option、rate-limit 错误形态、日志路径）。
2. Host MVP：**禁止**假设 `session/cancel`；`acp_cancel` 应对 OpenCode 降级（close session 和/或 kill child），并在通用层对「method not found」容错。
3. **第一行业务代码之前**：`is_acp_runtime` 分叉必须在所有 `get_adapter` 路径之前（DEF-001）。
4. Descriptor 钉死：`command=opencode` `args=["acp"]`；cwd 用 spawn cwd 或 `--cwd`；模型建议在 session/new 后 `session/set_config_option`。
5. 真连 prompt 被限流时：错误事件要上浮到 UI（`-32603` + stderr），不得挂死 Running。
6. 完成后：`phase_gate=dev_done` + log + **直接 send Test** 复测。

---

## 6. 残留未测 / 风险

- 成功 `agent_message_chunk` 正文（rate limit）
- tool_call / request_permission / fs/* 真触发
- `session/resume` 完整参数
- `session/fork`
- 官方 `agent-client-protocol` crate 与长会话（Phase1）
- UI 全路径（无代码）
- 多 tab 串台（无代码）

---

## 7. 工件索引

- 本报告：`docs/acp-dev-log/20260817-0356-test.md`
- 原始 NDJSON：`docs/acp-dev-log/20260817-0355-test-opencode-acp-raw.ndjson`
- Dev 并行：`20260817-0350-phase0-opencode-raw.json`、`20260817-0355-phase0-opencode-extended.json`
