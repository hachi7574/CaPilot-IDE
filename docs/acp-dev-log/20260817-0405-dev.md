# Dev log — Phase 0

time: 2026-08-17T04:05+08:00
role: dev (ad172813-30c1-4bea-82fe-ddfa1e4b0885)
phase: 0
phase_gate_after: dev_done
commit: 94efdf896

## 交付
1. 本机实测 `opencode acp`（1.18.18）全链路：initialize → session/new → set_config_option(model) → session/prompt → session/cancel → session/load。
2. 成功 prompt：模型 `opencode/nemotron-3.5-lightning-free`，回复 `pong`，`stopReason=end_turn`；工具轮次见 `tool_call`/`tool_call_update`（bash/ls）。
3. 附录 A.2 已填入 `docs/acp-runtime-plan.md`。
4. CI mock：`src-tauri/tests/fixtures/mock_acp_agent.py`（stdio NDJSON）。
5. 原始摘要：`docs/acp-dev-log/20260817-0405-phase0-summary.json` 等。

## 关键实现含义（给 Phase 1）
- Launch：`opencode acp`，cwd 用 `--cwd` 或 process cwd；**不要** TUI env/插件。
- `session/new` params 需 `mcpServers`（可 `[]`）。
- 换模型走 `session/set_config_option`，不是 CLI flag。
- 必须处理：`agent_message_chunk`、`agent_thought_chunk`、`tool_call*`、`usage_update`、`available_commands_update`（可忽略展示）、JSON-RPC 业务错误（限流）。
- `loadSession=true`；cancel → `stopReason: cancelled`。
- 本轮未见到 `request_permission`，Host 仍要实现。

## 自测
```bash
# mock
printf '%s\n' \
  '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":1,"clientCapabilities":{},"clientInfo":{"name":"t","version":"0"}}}' \
  '{"jsonrpc":"2.0","id":2,"method":"session/new","params":{"cwd":"/tmp","mcpServers":[]}}' \
  '{"jsonrpc":"2.0","id":3,"method":"session/prompt","params":{"sessionId":"sess_mock_1","prompt":[{"type":"text","text":"hi"}]}}' \
| python3 src-tauri/tests/fixtures/mock_acp_agent.py
# 真机日志见同目录 json
```

## 建议 Test 关注（仅参考）
- mock 脚本可独立跑、stdout 合法 NDJSON、无裸日志。
- 附录 A.2 与 summary json 一致、可复现 launch 命令。
- 确认未改 LeftSidebar / 未引入业务 Host 代码超 scope（本 phase 仅摸底+fixture+文档）。
