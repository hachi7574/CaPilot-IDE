# Test Report — 2026-08-17 04:10 (+08)

agentId: `d62ccd50-e114-4625-a058-6bb75a76dad6`  
role: Test  
trigger: Dev `phase_gate=dev_done` / commit `94efdf896`  
current_phase: 0 → **test_passed**

## 0. 实际 diff（审查对象）

```text
94efdf896 docs(acp): phase 0 — OpenCode ACP probe, mock fixture, design baseline
```

| 路径类 | 结论 |
| --- | --- |
| `docs/acp-runtime-plan.md` + 附录 A.2 | 有；与 summary 大体一致 |
| `docs/acp-dev-log/*phase0*` | 有原始/摘要日志 |
| `src-tauri/tests/fixtures/mock_acp_agent.py` | 新增 CI mock |
| `src-tauri/src/**` / `ui/**` 业务 | **未改**（范围正确） |
| `LeftSidebar.tsx` | **不在 commit 内** |

无 Host/Composer 实现 → §12 UI 项本 phase **不适用**（非失败）。

---

## 1. 本轮自拟多角度计划

| # | 角度 | 做法 |
| --- | --- | --- |
| A | 交付范围/越权 | `git show --name-only`：禁止业务 Host、禁 LeftSidebar |
| B | 文档↔日志一致性 | 附录 A.2 vs `0405-phase0-summary.json` vs `0400-tool-cancel.json` |
| C | Mock 功能/协议 | 独立 stdin 驱动：init/new/prompt/load/auth/unknown |
| D | Mock 对抗 | cancel 形态、permission 关键字、坏 method、stdout 纯 NDJSON |
| E | 锚点 OpenCode 独立复验 | **不信任** Dev「cancel 成功」字面：分别打 **request(带 id)** vs **notification(无 id)**；并复验 set_model + prompt pong + load |
| F | 回归 | 全量 `cargo test`；`pnpm exec tsc --noEmit` |
| G | 预埋 Phase1 风险 | `get_adapter` fallback；设计正文 `acp_cancel→session/cancel` 是否写清 notification |

---

## 2. 结果摘要

### A 范围 — PASS
- commit 仅 docs + `mock_acp_agent.py`
- 无 `agent_runtime/acp/`、无 `AcpBridge`

### B 文档一致性 — PASS（有批注）
- A.2 与 summary：capabilities / set_config_option / loadSession / rate-limit / tool_call 一致
- cancel 行写明 **notification** → prompt `stopReason: cancelled` — **与独立复验一致**
- 设计正文 §1 流程 / §3.7 `acp_cancel` 仍像「普通 RPC request」，**未**强调 OpenCode 必须无 `id`（见 DEF-002）

### C Mock happy path — PASS
```bash
printf '%s\n' '<init>' '<session/new>' '<session/prompt hi>' \
  | python3 src-tauri/tests/fixtures/mock_acp_agent.py
```
- stdout **仅**合法 NDJSON；stderr 空
- 序列：init result → new `sess_mock_1` → 2× `agent_message_chunk` → `usage_update` → `stopReason: end_turn`
- `session/load` / `authenticate` / unknown → `-32601` 正常

### D Mock 对抗 — PASS with majors（不挡 Phase0 出口）

| 用例 | 结果 |
| --- | --- |
| cancel notification（无 id） | mock `pass`，无输出（可接受为 no-op） |
| cancel **request（带 id）** | mock 亦 `pass`，**不**回 error/result — Host 若 await 会挂（DEF-005） |
| prompt 含 `permission` 且 stdin 结束 | 发出 `session/request_permission` 后 `recv None` 跳出，仍 `end_turn`（不永挂） |
| permission + 客户端应答 | 可见 permission 请求；本 harness 未稳定收到最终 `id=3` result（DEF-005 minor 分支） |
| 非 JSON / 未知 method | 未知 method → `-32601` |

**Phase0 硬门槛**（编排 §2）：mock 可独立跑 init/new/prompt 合法 NDJSON → **满足**。

### E 锚点 OpenCode 1.18.18 — PASS（关键独立发现）

| 步骤 | 结果 |
| --- | --- |
| initialize / session/new | OK |
| `session/set_config_option` model=`opencode/nemotron-3.5-lightning-free` | OK |
| `session/cancel` **作为 request（含 id）** | **`-32601 Method not found`** |
| `session/cancel` **作为 notification（无 id）** 于 prompt 中 | prompt result **`stopReason: "cancelled"`** ✅ |
| prompt「Reply with exactly: pong」 | **`end_turn`**；updates 含 `agent_thought_chunk` / `agent_message_chunk` / `usage_update` |
| `session/load` | OK；result keys=`configOptions`（无 sessionId 字段，与 A.2 一致） |

→ 推翻 Test 上轮「OpenCode 完全无 cancel」的粗结论；**精确结论：仅 notification 形态有效**。Dev 附录正确；我方复验确认。

### F 回归 — PASS
- `cd src-tauri && cargo test` → **206 passed, 0 failed, 1 ignored** + daemon_smoke 3
- `pnpm exec tsc --noEmit` → exit 0

### G Phase1 预埋 — 记缺陷不挡 Phase0
- `get_adapter` `_ => ClaudeAdapter` 仍在（DEF-001）
- `acp_cancel` 若用 **带 id 的 request** 对 OpenCode 必挂/必错（DEF-002）

---

## 3. Defects

| id | phase | severity | title | repro | status |
| --- | --- | --- | --- | --- | --- |
| DEF-001 | 1 | major | `get_adapter` 未知 id 默认 Claude；`acp:*` 无前置分叉 | `runtimes/mod.rs` `_ => ClaudeAdapter`；`lib.rs` 多处直接 `get_adapter` | open |
| DEF-002 | 0/1 | **blocker（Phase1 Host）** | OpenCode `session/cancel` **仅 notification（无 id）**；带 id 的 request → `-32601`。设计 §3.7/`acp_cancel` 未写死此线序 | 见本报告 §2.E；对比附录 A.2（已写 notification）与正文 command 表 | open |
| DEF-005 | 0/1 | major | mock：`session/cancel` 带 id 时静默 no-op（不 error 也不模拟 cancelled）；permission 交互路径未稳定返回 prompt result | `mock_acp_agent.py` elif cancel: pass；permission 分支 | open |
| DEF-003 | 0 | closed | 附录未填 / 无成功 prompt — 已由 94efdf896 + 本轮复验关闭 | — | closed |
| DEF-004 | 0 | closed | 默认 model 限流 — A.2/summary 已记录 + 推荐 free 模型 | — | closed |

---

## 4. Phase 0 出口判定

编排 Phase0 硬门槛：
1. 附录 A + 可复现 NDJSON/摘要日志 — **是**
2. mock 独立 init→new→prompt 合法 NDJSON — **是**
3. 未破坏 PTY / tsc — **是**
4. 范围未污染业务 UI — **是**

**phase_gate = test_passed**  
**current_phase 仍为 0**（递增由 Watcher 在 test_passed 后执行；Test 不擅自 +1，但 handoff 明确可进 Phase 1）

DoD / §12：**不**因本 phase 勾选（无 UI/Host）。

---

## 5. 给 Dev 的 Phase 1 强制约束（测试侧）

1. **`acp_cancel` → 对 OpenCode 发 JSON-RPC notification：`{"jsonrpc":"2.0","method":"session/cancel","params":{"sessionId"}}`（禁止 id）**；通用层对「cancel method not found」要降级（close/kill）。
2. **所有 `get_adapter` 前 `is_acp_runtime`**（DEF-001）。
3. mock 建议：cancel request → 返回/模拟 `cancelled`；permission 路径保证最终 `stopReason`（DEF-005）。
4. 默认/推荐模型写入 descriptor 或 session 启动后 `set_config_option`（nemotron free 本轮可用）。
5. 完成 Host 后 `dev_done` + **直接 send Test**。

---

## 6. 残留未测（明示）

- `request_permission` 真机仍 0 次（A.2 已声明）— Phase3
- `session/fork` / `session/resume` 完整参数
- client `fs/*` 回调（本轮工具走 agent 内置）
- UI / Composer / 多 tab（无代码）
- `agent-client-protocol` crate 长会话嵌入

---

## 7. 命令索引

```bash
# mock
printf '%s\n' \
  '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":1,"clientCapabilities":{},"clientInfo":{"name":"t","version":"0"}}}' \
  '{"jsonrpc":"2.0","id":2,"method":"session/new","params":{"cwd":"/tmp","mcpServers":[]}}' \
  '{"jsonrpc":"2.0","id":3,"method":"session/prompt","params":{"sessionId":"sess_mock_1","prompt":[{"type":"text","text":"hi"}]}}' \
| python3 src-tauri/tests/fixtures/mock_acp_agent.py

cd src-tauri && cargo test
pnpm exec tsc --noEmit
# OpenCode cancel 形态见本机 python 复验（notification vs request）
```
