# Dev Report — 2026-08-17 ~04:15–04:45 (+08)

agentId: `ad172813-30c1-4bea-82fe-ddfa1e4b0885`  
role: Dev  
trigger: Test Phase0 `test_passed` + 指令继续 Phase1 Host  
current_phase: **1** → `dev_done`

## 交付

### 模块 `src-tauri/src/agent_runtime/acp/`
| 文件 | 职责 |
| --- | --- |
| `mod.rs` | `is_acp_runtime` / `strip_acp_prefix` / `acp_runtime_id` |
| `descriptor.rs` | 默认 `opencode`→`acp:opencode`；读 `~/CaPilot/acp-agents.json` 合并 |
| `registry.rs` | → `RuntimeInfo{transport:"acp"}` 并入 list |
| `host.rs` | 普通 process 管道 + NDJSON；initialize / session/new|load / prompt；**cancel = notification 无 id（DEF-002）** |
| `bridge.rs` | `AcpBridge` 会话表；prompt/cancel/kill；Tauri `acp://event` sink |
| `events.rs` | 前端 DTO + `VecEventSink` 测试 |
| `permission.rs` | pending permission 板（UI 闭环 Phase3） |

### `lib.rs` 分叉（DEF-001）
- `agent_spawn` / `agent_resume`：**先** `is_acp_runtime` → `build_and_spawn_acp`，**绝不** `get_adapter("acp:…")`
- `build_and_spawn` 入口再 guard 一次
- `agent_write` 对 ACP id **明确报错**
- `agent_kill` / `sessions_delete` 双桥
- `agent_context_usage` / `agent_status_read` / `agent_set_session_config` / `agent_switch_runtime` 均避开 acp→Claude
- 新 commands：`acp_prompt` / `acp_cancel` / `acp_respond_permission`
- `runtime_list_available` 追加 ACP descriptors；PTY 填 `transport:"pty"`
- `RuntimeInfo.transport` 字段（adapter.rs + 前端 store 可选）

### Mock（DEF-005 部分）
- `session/cancel` **带 id** → `-32601`（对齐 OpenCode）
- notification 仍静默

### 未做（有意，属后 phase）
- 前端 AcpSessionPanel / Composer 分发（Phase 2）
- permission UI 卡 + fs/read 沙箱实现（Phase 3；Host 已能 emit permission_request）
- Settings CRUD 写回（descriptor 读路径已就绪）
- 未加 `agent-client-protocol` crate：自研 NDJSON（设计 §3.2 / §9 允许）

## 验证

```text
cd src-tauri && cargo test acp   → 9 passed
cd src-tauri && cargo test       → 215 passed + daemon_smoke 3
pnpm exec tsc --noEmit           → exit 0
```

OpenCode 1.18.18 无 UI 烟雾：
- initialize + session/new OK
- set_config_option free model OK
- `session/cancel` **notification** → prompt `stopReason: cancelled` ✅
- mock cancel-with-id → `-32601` ✅

不注入 `OPENCODE_TUI_CONFIG`。未改 `LeftSidebar.tsx`。

## Defects 认领

| id | 本轮 |
| --- | --- |
| DEF-001 | **fixed** — is_acp 前置分叉 + 多 command 守卫 |
| DEF-002 | **fixed** — Host cancel 仅 notification |
| DEF-005 | **partial** — cancel-with-id 现报 -32601；permission 交互仍 Phase3 稳定 |

## 请 Test

重测 Phase 1：mock 集成、`cargo test` 全量、grep `is_acp_runtime`/`get_adapter`、cancel 线序、list 含 `acp:opencode`。  
完成后 **直接 send Dev**。
