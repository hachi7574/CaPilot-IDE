# Test Report — 2026-08-17 ~04:55 (+08)

agentId: `d62ccd50-e114-4625-a058-6bb75a76dad6`  
role: Test  
trigger: Dev Phase1 `dev_done` / commit `145072f9a`  
current_phase: **1** → **test_passed**

## 0. 实际 diff（审查对象）

```text
145072f9a feat(acp): phase 1 — Host MVP, is_acp fork, notification cancel
```

| 路径类 | 结论 |
| --- | --- |
| `src-tauri/src/agent_runtime/acp/*` | 新增 Host/bridge/descriptor/registry/events/permission |
| `src-tauri/src/lib.rs` | spawn/resume/write/kill/list/status/usage/config/switch 分叉 + `acp_*` commands |
| `adapter.rs` / `store.ts` | `RuntimeInfo.transport` |
| `mock_acp_agent.py` | cancel-with-id → `-32601` |
| `ui/**` 业务面板 | **未改**（无 AcpSessionPanel；属 Phase2） |
| `LeftSidebar.tsx` | **不在 commit 内** ✅ |

范围正确：Host MVP only，无 UI 污染。

---

## 1. 本轮自拟多角度计划

| # | 角度 | 做法 |
| --- | --- | --- |
| A | 交付范围/越权 | `git show --name-only 145072f9a`；禁 LeftSidebar；禁产品 UI 业务 |
| B | DEF-001 静态 | 全部 `get_adapter` 位点：ACP 入口是否先 `is_acp_runtime` |
| C | DEF-002 静态+动态 | host `cancel` → `HostCmd::Notify` 无 id；OpenCode 真机 notification vs request |
| D | DEF-005 mock | cancel-with-id → -32601；notification 静默；prompt end_turn |
| E | 单元/集成 | `cargo test acp` + bridge mock prompt/cancel/kill |
| F | 回归 | 全量 `cargo test` + daemon_smoke + `tsc --noEmit` |
| G | 命令面 | `agent_write` ACP 报错；list 含 `acp:opencode` transport=acp；`acp_prompt/cancel/respond_permission` 注册 |

---

## 2. 结果摘要

### A 范围 — PASS
- 15 files；无 `LeftSidebar.tsx`；无 Composer/AcpSessionPanel
- 仅 `store.ts` 可选 `transport` 字段

### B DEF-001 — PASS（fixed）
关键路径均有前置分叉或显式拒绝：

| 位点 | 守卫 |
| --- | --- |
| `agent_spawn` ~640 | `is_acp_runtime` → `build_and_spawn_acp` |
| `build_and_spawn` ~246 | 入口再 guard |
| `agent_resume` ~746 | ACP → AcpBridge / rebuild |
| `agent_write` ~1055/1062 | bridge.contains **或** rec.runtime is_acp → 明确 Err |
| `agent_context_usage` ~862 | is_acp → Ok(None) **先于** get_adapter |
| `agent_status_read` ~987 | is_acp → Ok(None)（且 live bridge 映射在前） |
| `agent_set_session_config` ~1212 | is_acp 分支不调 get_adapter |
| `agent_switch_runtime` ~1146 | to/from ACP → Err（Phase1 有意） |
| `runtime_list_available` ~2024 | `get_adapter` **仅** `known_runtimes()` PTY id；ACP 经 `registry::list_runtime_infos` 追加 |
| `sessions_delete` | 双桥 kill（PTY+ACP） |

残留 `get_adapter`（686/692 spawn 后 resume 捕获、891 usage blocking、1151 switch、2024 list）均在 **非 ACP** 控制流之后，**不会**把 `acp:*` 映射成 Claude。

`is_acp_runtime` = `starts_with("acp:")`（大小写敏感；`opencode` PTY id 不误伤）。

### C DEF-002 — PASS（fixed）
**源码：**
```rust
// host.rs cancel
HostCmd::Notify {
    method: "session/cancel".to_string(),
    params: json!({ "sessionId": sid }),
}
// writer:
json!({ "jsonrpc": "2.0", "method": method, "params": params }) // 无 id
```

**OpenCode 1.18.18 独立复验（本轮）：**
| 形态 | 结果 |
| --- | --- |
| `session/cancel` **request（含 id=50）** | `-32601 Method not found` |
| prompt 中途 **notification（无 id）** | prompt result **`stopReason: "cancelled"`** ✅ |

（set_config_option 参数名本轮用错 `id` 而非 `configId` → -32602；**不影响** cancel 结论，未依赖 model 设置即已 cancelled。）

### D DEF-005 — PASS partial（与 Dev 一致）
Mock 独立 stdin：
- cancel **request id=99** → `error.code=-32601` “…use notification” ✅
- cancel **notification** → 无 stdout 行（静默）✅
- prompt → chunks + `stopReason: end_turn` ✅
- permission 交互稳定仍属 **Phase3**（不挡 Phase1）

### E cargo test acp — PASS
```text
cargo test acp → 9 passed
```
含：prefix、descriptor defaults/merge、registry `acp:opencode`+transport=acp、mock prompt chunks/end_turn、cancel notification 不挂、bridge start/prompt/kill。

### F 回归 — PASS
```text
cd src-tauri && cargo test
  → 215 passed; 0 failed; 1 ignored + daemon_smoke 3 passed
pnpm exec tsc --noEmit → exit 0
```

### G 命令面 — PASS
- `agent_write` 文案：`ACP session does not accept PTY writes; use acp_prompt instead`
- `runtime_list_available` 追加 ACP；registry 单测断言 `acp:opencode` / `transport=="acp"`
- `acp_prompt` / `acp_cancel` / `acp_respond_permission` 已 `generate_handler!`
- PTY `runtime:"opencode"` 与 `acp:opencode` id 空间分离

---

## 3. Defects

| id | phase | severity | title | status |
| --- | --- | --- | --- | --- |
| DEF-001 | 1 | major | get_adapter 未知 id→Claude；acp 无前置分叉 | **closed**（本轮确认 fixed） |
| DEF-002 | 0/1 | blocker(P1) | OpenCode cancel 仅 notification | **closed**（源码+真机确认） |
| DEF-005 | 0/1 | major | mock cancel-with-id；permission 不稳 | **partial** → cancel-with-id **closed**；permission **open→Phase3** |
| DEF-003/004 | 0 | — | 附录/限流 | closed（既往） |

### 非挡板观察（记给 Phase2/3，不设新 DEF 除非再现）
- `agent_switch_runtime` 明确拒绝 ACP 互转 — 产品化前需设计
- OpenCode `session/set_config_option` 字段为 `configId`（非 `id`）；Host 若后续 set model 须对齐附录 A.2
- permission UI / fs 沙箱未测（Phase3）
- 无 UI：§12 D*/C* 本 phase **N/A**

---

## 4. Phase 1 出口判定

编排 Phase1 硬门槛（对照 status DoD Host 层 + Dev handoff）：
1. acp 模块 + descriptor `acp:opencode` — **是**
2. lib 分叉 is_acp **先于** get_adapter（DEF-001）— **是**
3. cancel = notification 无 id（DEF-002）+ 真机 cancelled — **是**
4. mock 集成 + cancel-with-id -32601 — **是**
5. `cargo test` 全绿 + tsc 0 — **是**
6. `agent_write` ACP 拒绝 — **是**
7. LeftSidebar 未动 — **是**
8. 未做 UI（有意）— **OK**

**phase_gate = test_passed**  
**current_phase 仍为 1**（+phase 由 Watcher 在 test_passed 后执行；Test 不擅自 +1）

DoD §12 UI 项：**不**因本 phase 勾选。

---

## 5. 给 Dev 的 Phase 2 约束（测试侧）

1. 前端必须用 `isAcpRuntime` / `transport==="acp"`（或 id `acp:` 前缀），**禁止** `runtime === "opencode"` 判断 ACP。
2. Composer 走 `acp_prompt`；Stop → `acp_cancel`；**禁止** `agent_write` 对 ACP tab。
3. 隐藏 PTY OpenCode 方言控件（⚡/Build/F12）。
4. 多 tab：事件按 agent id 隔离（`acp://event` envelope）。
5. 完成后 `dev_done` + **直接 send Test**。

---

## 6. 残留未测（明示）

- permission 真机 + UI 卡（Phase3）
- fs/read 出界拒绝（Phase3）
- session/load resume 完整参数矩阵
- 第二个 ACP agent 仅 descriptor 冒烟（延伸）
- UI §12 D*/C*（Phase2）

---

## 7. 命令索引

```bash
cd src-tauri && cargo test acp   # 9
cd src-tauri && cargo test       # 215 + daemon_smoke 3
pnpm exec tsc --noEmit
# mock cancel shapes — see body §2.D
# OpenCode cancel notification → stopReason cancelled — see body §2.C
```
