# Dev Report — 2026-08-17 ~05:10–05:35 (+08)

agentId: `ad172813-30c1-4bea-82fe-ddfa1e4b0885`  
role: Dev  
trigger: Test Phase1 `test_passed` + 指令直接开 Phase 2  
current_phase: **2** → `dev_done`

## 交付

### 新建
| 文件 | 职责 |
| --- | --- |
| `ui/state/runtimeTransport.ts` | `isAcpRuntime` / `runtimeTransport`（**仅** `acp:` 前缀；禁 `=== opencode`） |
| `ui/state/acpTypes.ts` | 会话 VM + `AcpEventPayload` 线型 |
| `ui/state/acpEvents.ts` | 全局 `listen("acp://event")` → `applyAcpEvent` |
| `ui/components/acp/AcpSessionPanel.tsx` | 结构化消息/工具/plan/turn/权限卡 MVP |

### 改动
| 文件 | 要点 |
| --- | --- |
| `ui/state/store.ts` | `acpSessions`；`markAcpLive` / `applyAcpEvent`；`effectiveAgentStatus` ACP 分支；drop/sleep 清 live |
| `ui/state/agentActions.ts` | spawn ACP 不挂 PTY channel；`sendPromptToAgent` → `acp_prompt`；`cancelAcpTurn`；todo 路径同 |
| `ui/components/layout/ContentArea.tsx` | `isAcpRuntime` → AcpSessionPanel；`openCodeTabs` 仍仅 PTY `opencode` |
| `ui/components/layout/Composer.tsx` | Stop→`acp_cancel`；Esc cancel ACP；隐藏 ⚡/Build/模型 TUI；Ctrl+T/Shift+Tab 方言仅 PTY；perm/model 对 ACP 只持久化 |
| `ui/components/layout/TabBar.tsx` | connected = PTY channel **或** `acpSessions.live` |
| `ui/App.tsx` | `useAcpEvents()` |
| `ui/App.css` | `.acp-*` + `.ul-stop-btn` |

### 有意未做（后 phase）
- permission Host 自动策略 / fs 沙箱（Phase 3；UI 卡已可点 allow/reject）
- Settings descriptor CRUD、消息持久化、markdown 渲染
- OpenCode 真机 UI 烟雾（交 Test；本环境不做长 `tauri dev`）

### 未改
- `LeftSidebar.tsx`（用户改动保留）
- 后端 Host（Phase 1 已过）

## 验证

```text
pnpm exec tsc --noEmit     → exit 0
cd src-tauri && cargo test acp → 9 passed
```

## 请 Test

多角度：  
1. grep / 走读：`isAcpRuntime` 使用点；Composer 无 ACP→`agent_write`；`=== "opencode"` 仅 PTY。  
2. `acp:opencode` spawn → 非 xterm → `acp_prompt` 流式 chunk → Stop/`acp_cancel`。  
3. 多 tab 事件不串；PTY opencode 回归 ⚡/Build 仍在。  
4. tsc + cargo acp。  

完成后 **直接 send Dev**（或 phase_gate）。
