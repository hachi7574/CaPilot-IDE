# ACP Dev Log — 2026-08-17T06:16 (Phase 5 DEF-011 silent chat)

role: dev  
agentId: ad172813-30c1-4bea-82fe-ddfa1e4b0885  
phase: 5  
gate_out: dev_done

## Summary
DEF-011：修 FE 静默失败 + host 死会话误判 live + 默认/bootstrap 改 go flash + acp_prompt 自动 resume。goal_met 保持 false。

## Behavior changes
1. `emptyAcpSession().live` 默认 **false**（恢复会话不再假 live）。
2. `ensureAgentChannel` 调 `acp_session_alive`；host 无进程才 resume；失败 → 面板 error + notify。
3. `sendPromptToAgent`：**先** ensure + `acp_prompt`，成功后再画 user 气泡；sync 失败 → 面板 error + notify（不再「只有 user 气泡」）。
4. `Composer.handleSend` catch → notify + 面板 error（不再仅 console.error）。
5. `AcpBridge::contains/is_alive`：死子进程从表中剔除并 emit 人话 error。
6. `acp_prompt`：bridge 无活进程时从 sessions.db **自动 resume**；若 DB model 为 zen free 则改 pin `opencode-go/deepseek-v4-flash`。
7. 默认 catalog / `pick_bootstrap_model`：**go flash 优先**（zen free 仍可选）。
8. rate-limit 检测扩 429/quota；`prompt_async` 失败 **humanize** 后 emit Error。
9. 新增 Tauri 命令 `acp_session_alive`。

## Paths
- `src-tauri/src/agent_runtime/acp/bridge.rs`
- `src-tauri/src/agent_runtime/acp/host.rs`
- `src-tauri/src/agent_runtime/acp/registry.rs`
- `src-tauri/src/lib.rs`
- `ui/state/acpTypes.ts`
- `ui/state/agentActions.ts`
- `ui/state/store.ts`
- `ui/components/layout/Composer.tsx`
- `ui/components/acp/AcpSessionPanel.tsx`

## Self-check
```
cd src-tauri && cargo test acp   # 24 passed
cargo test --lib opencode_acp_real_prompt_smoke -- --nocapture
# → ok, model=Some("opencode-go/deepseek-v4-flash")
pnpm tsc --noEmit               # 0
```

## Limits
- 未启动 GUI / 未做 Wayland 点击；用户须加载 **本 worktree 构建**（`pnpm tauri dev` 或安装 debug），`/usr/bin/capilot-ide` 04:56 仍是旧包。
- goal_met 保持 false，待用户 UI 手点或 Test 产品门禁。
- LeftSidebar 用户 dirt 未动。

## Handoff
Direct send Test only.
