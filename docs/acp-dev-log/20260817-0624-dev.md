# ACP Dev Log — 2026-08-17T06:24 (Phase 5 product assist)

role: dev  
agentId: ad172813-30c1-4bea-82fe-ddfa1e4b0885  
phase: 5  
gate_out: test_passed（代码）; product_gate 待用户手点  
goal_met: false

## Context
Test 06:30 对 commit `6d6e5b0ab` 代码复测 **test_passed**。本轮 Dev 协助加载新构建，不改代码逻辑，不 goal_met。

## Actions
1. 确认 debug bin mtime **06:15/06:23** 含 `acp_session_alive` / go flash 符号；`/usr/bin/capilot-ide` 仍 **04:56**。
2. `pnpm build` 产出 `dist/`（含 DEF-011 FE：`acp_session_alive` 调用链）。
3. 启动 **`pnpm tauri dev`**（本 worktree）→ vite `http://localhost:1420/` + `target/debug/capilot-ide`（pid 见 `/tmp/capilot-tauri-dev-def011.log`）。
4. RUNBOOK §1 增补 **「ACP 验证：勿用旧系统包」** 说明。

## User 手点清单
1. 聚焦 **本 worktree tauri dev 窗口**（勿用系统菜单里的旧 CaPilot / `/usr/bin`）。
2. 新建或打开 runtime=`acp:opencode` 会话。
3. Composer 发短消息（如 `ping`）；确认模型优先 **go flash**。
4. 期望：面板出现助手文本，或**人话错误**（不再静默）。
5. 成功后书面回复 / 再唤 Test 关 DEF-011；**此前 goal_met=false**。

## Limits
- Wayland 无法代点；本机无可靠 UI 注入。
- 未 send Watcher。
- LeftSidebar 未动。
