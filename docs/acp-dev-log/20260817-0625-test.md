# Test Note — 2026-08-17 ~06:25 (+08) — Dev product-assist 现场核对

agentId: `d62ccd50-e114-4625-a058-6bb75a76dad6`  
role: Test  
trigger: Dev finish — tauri dev 已起 + RUNBOOK 勿用旧包（`bca950607`）  
gate: **test_passed 不变** · **goal_met=false**

## 核对（非手点）

| 项 | 事实 |
| --- | --- |
| HEAD | `bca950607`（docs） atop `6d6e5b0ab`（DEF-011 fix） |
| `pnpm tauri dev` | **running**（pid ~168232 链） |
| GUI | `target/debug/capilot-ide` **pid 168359** |
| debug mtime | **06:23**（含 011 修复） |
| `/usr/bin/capilot-ide` | 仍 **04:56** + daemon 5330（旧包，勿用） |
| vite | `:1420` ready（log） |
| log | `/tmp/capilot-tauri-dev-def011.log` — 启动干净，无 ACP panic |
| Wayland 手点 | **本 Test 未做**（无输入注入） |

## 判定

- Dev product-assist（拉起正确构建）**属实** → 011c 缓解中（通道已备，用户须点对窗口）。
- **product_gate 仍缺：** 用户在 tauri dev 窗口对 `acp:opencode` 发消息并见助手回复（或人话错误 / 书面签字）。
- **禁止**因「进程在跑」关 DEF-011 或 `goal_met=true`。

## 出口

不变：`phase_gate=test_passed` · `phase=5` · `goal_met=false`  
未 send Watcher。等用户手点结果后再关票。
