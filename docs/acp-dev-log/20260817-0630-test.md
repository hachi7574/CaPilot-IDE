# Test Report — 2026-08-17 ~06:30 (+08) — DEF-011 修复复测

agentId: `d62ccd50-e114-4625-a058-6bb75a76dad6`  
role: Test  
trigger: Dev `dev_done` commit `6d6e5b0ab` / log `20260817-0616-dev.md`  
current_phase: **5**  
gate: **test_passed**（代码切片）· **goal_met=false**（产品手点未过）

## 0. 实际 diff

```text
6d6e5b0ab fix(acp): phase 5 DEF-011 — silent chat, live/resume, go flash default
```

| 路径 | 结论 |
| --- | --- |
| `acpTypes.ts` | `emptyAcpSession.live` → **false** |
| `agentActions.ts` | `acp_session_alive`；ensure 失败上屏；prompt **先** invoke 再 user 气泡 |
| `Composer.tsx` | catch → notify + 面板 error |
| `AcpSessionPanel.tsx` | resume 失败 → 面板 error |
| `store.ts` | error 文案含进程退出/not found → `live=false` |
| `bridge.rs` | `is_alive` 剔死进程 + emit；prompt_async humanize Error |
| `host.rs` | rate 检测扩 429/quota；bootstrap go 优先；humanize 扩 |
| `registry.rs` | catalog 默认 go flash |
| `lib.rs` | `acp_session_alive`；`acp_prompt` 死进程自动 resume + zen free→go pin |
| LeftSidebar | **不在 commit** |

## 1. 本轮计划（自拟，非 Dev 清单照抄）

| # | 角度 | 做法 |
| --- | --- | --- |
| A | DEF-011a 错误上屏 | 读 sendPrompt/Composer 顺序与 catch |
| B | DEF-011b live/resume | empty live；alive RPC；bridge 剔死；store 清 live |
| C | 后端 auto-resume / 模型 | acp_prompt DB resume；zen free pin go；catalog 默认 |
| D | 回归门禁 | cargo acp + smoke + tsc |
| E | 011c 装载 | 系统包 mtime vs debug；进程 |
| F | product_gate | 有无 GUI 手点 — **不得 goal_met** |

## 2. 结果

### A DEF-011a — PASS（代码路径 fixed）
- `sendPromptToAgent` ACP：`ensure` → **`acp_prompt`** → 成功后才 `message_chunk user` + busy  
- invoke 失败：`applyAcpEvent error` + idle + `notify` + rethrow  
- `handleSend` catch：`notify("发送失败")` + 面板 error（不再仅 console）  
- 证伪点「先乐观气泡后静默失败」**已消除**

### B DEF-011b — PASS（代码路径 fixed）
- `emptyAcpSession().live === false`  
- `ensureAgentChannel`：`invoke("acp_session_alive")`；host 活才 markLive；死则清 live 再 `agent_resume`；resume 失败面板+notify  
- `AcpBridge::contains` → `is_alive`；死 child remove+kill+中文 Error  
- `applyAcpEvent` error：匹配 进程已退出 / process exited / session not found → `live=false`  
- `acp_session_alive` 已注册  

### C auto-resume / 默认模型 — PASS（代码+单测）
- `acp_prompt`：`!is_alive` → DB get → `build_and_spawn_acp`；model 为 zen free 或 None → pin **`opencode-go/deepseek-v4-flash`**  
- catalog / `pick_bootstrap_*` 单测：go flash 默认  
- rate-limit 子串扩 429/quota/too many requests；humanize 文案更新  
- `prompt_async` 失败：`humanize_acp_error` + Error + idle（经 sink，不依赖 app handle 可选路径）

### D 回归 — PASS
```text
cargo test acp                         → 24 passed（含 opencode_acp_real_prompt_smoke）
pnpm exec tsc --noEmit                 → 0
```
smoke 在 acp filter 内 **ok**（约 4.5s 套件）。

### E DEF-011c 装载 — 仍 OPEN（产品）
| 项 | 值 |
| --- | --- |
| `/usr/bin/capilot-ide` | **04:56**（仍旧，无本修复） |
| `target/debug/capilot-ide` | **06:15**（含本修复） |
| 进程 | 仅系统 **daemon**；**无** tauri dev / debug GUI |

→ 用户若仍点系统包窗口，**代码修无效**。011c 不挡代码 gate，**挡 goal_met**。

### F product_gate — 未过
- 本环境无 Wayland 手点；Dev 明示未启 GUI。  
- **禁止 goal_met=true**。  
- DEF-011 总票：代码 mitigations **落地**；**用户手点成功前保持 open/mitigated**。

## 3. Defects

| id | sev | status |
| --- | --- | --- |
| DEF-011 | blocker | **mitigated in code**；待用户/新构建 UI 手点关闭 |
| DEF-011a | blocker | **fixed**（代码路径） |
| DEF-011b | blocker | **fixed**（代码路径） |
| DEF-011c | major | **open** — 须 `pnpm tauri dev` 或安装 debug；系统包 04:56 无效 |
| DEF-010 | minor | open（默认已改 go；zen 仍可选限流） |
| DEF-008/009 | — | mitigated in code；UI 手点仍待 |

无新开 DEF。LeftSidebar 未动。

## 4. 出口判定

| 项 | 结果 |
| --- | --- |
| 011a/b 代码修复可复验证据 | **PASS** |
| cargo acp 24 / tsc 0 / smoke | **PASS** |
| 011c 用户装载新构建 | **未完成** |
| 用户 UI 手点有回复 | **未完成** |
| goal_met | **false** |

**phase_gate = test_passed**（本切片代码出口满足）  
**current_phase = 5**（**不** +phase；product_gate 未齐）  
**goal_met = false**

## 5. 给 Dev

- 代码侧 DEF-011a/b **可收**；请推动用户/本机 **加载 worktree 构建** 后手点一轮（或书面签字）。  
- 手点成功前 **勿** goal_met。  
- 可选：文档/启动器提示勿用 04:56 系统包测 ACP。  
- 未 notify Watcher。

## 6. 命令索引

```bash
git show 6d6e5b0ab --stat
cd src-tauri && cargo test acp
pnpm exec tsc --noEmit
stat /usr/bin/capilot-ide src-tauri/target/debug/capilot-ide
```
