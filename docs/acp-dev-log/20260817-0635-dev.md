# ACP Dev Log — 2026-08-17T06:35 (Phase 2 DEF-006/007 fix)

role: dev  
agentId: ad172813-30c1-4bea-82fe-ddfa1e4b0885  
phase: 2  
gate_out: dev_done  
base_fail: commit `1088ba1cc` test_failed (DEF-006 blocker, DEF-007 minor)

## Summary
修复 Test 指出的 F1 主路径缺失：项目「+」/ TabBar「+」的 `TerminalTemplatePicker` 现在动态列出可用 ACP runtime（含 `acp:opencode`），点击走既有 `spawnTerminal` → `spawnAgent` ACP 分支。侧栏 runtime 切换列表隐藏 ACP，避免误调 `agent_switch_runtime`。Host `AcpBridge::prompt` 在 turn 开始 emit `status:running`（DEF-007）。

## Changes
| file | change |
| --- | --- |
| `ui/components/layout/TerminalTemplatePicker.tsx` | 读 `store.runtimes`；`transport==="acp" \|\| isAcpRuntime` 且 available → 动态菜单「ACP Agents」；`spawnTerminal` |
| `ui/state/store.ts` | `TermTemplate.runtime: string`（容纳 `acp:*`） |
| `ui/components/Icon.tsx` | `acp:opencode` → brand `opencode` |
| `ui/components/layout/LeftSidebar.tsx` | **仅** switch 列表 filter ACP + import `isAcpRuntime`；用户其它脏改未进 commit |
| `src-tauri/src/agent_runtime/acp/bridge.rs` | `prompt` 前 emit running；err 回 idle |

## Non-goals / preserved
- 未动用户 LeftSidebar 空白区右键/新建项目等脏改（工作区仍在，未 stage）。
- 未把 ACP 接进 `agent_switch_runtime`；未混用 PTY `opencode`。
- Composer 空发送仍默认 claude（Test 主要求是 Picker 一等入口）。

## Verification
- `pnpm tsc --noEmit` → 0
- `cd src-tauri && cargo test acp` → 9 passed

## Handoff
直连 Test `d62ccd50-e114-4625-a058-6bb75a76dad6` 复测 Phase 2 / F1。
