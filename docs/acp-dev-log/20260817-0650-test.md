# Test Report — 2026-08-17 ~06:50 (+08)

agentId: `d62ccd50-e114-4625-a058-6bb75a76dad6`  
role: Test  
trigger: Dev Phase2 retest after DEF-006/007 / commits `243c97de1` + `dec0dd0e0`  
base_fail: `1088ba1cc` test_failed  
current_phase: **2** → **test_passed**

## 0. 实际 diff

```text
243c97de1 fix(acp): phase 2 — DEF-006 primary spawn UX + DEF-007 running status
dec0dd0e0 docs(acp): phase 2 DEF-006/007 fix → dev_done handoff
```

| 路径 | 结论 |
| --- | --- |
| `TerminalTemplatePicker.tsx` | 动态 ACP 段 + spawnTerminal |
| `store.ts` | `TermTemplate.runtime: string` |
| `Icon.tsx` | `acp:opencode` → brand opencode |
| `LeftSidebar.tsx` | **仅** switch 列表 filter ACP（+6/−1）；用户其它脏改未进 commit |
| `acp/bridge.rs` | prompt 开始 `Status{running}`；err → idle |
| docs | status + dev log |

---

## 1. 本轮计划（针对 fail 复测）

| # | 角度 | 做法 |
| --- | --- | --- |
| A | DEF-006 F1 主路径 | Picker 过滤条件、菜单分区、点击 → spawnAgent(acp) |
| B | spawnTerminal 无 ACP agent_write | command 仅 bash 写 |
| C | 侧栏 switch 不误调 ACP | filter `!isAcpRuntime && transport!==acp` |
| D | DEF-007 | bridge.prompt emit running / err idle；prompt_async 走 prompt |
| E | 回归 panel/composer 纪律 | 快速确认 isAcp / 方言隐藏仍在（承 1088ba1cc） |
| F | tsc + cargo acp | 硬门槛 |
| G | LeftSidebar 越权 | commit 仅 filter；WT 脏改保留 |

---

## 2. 结果

### A DEF-006 / F1 — PASS（closed）
`TerminalTemplatePicker`：
- `runtimes.filter(available && (transport==="acp" \|\| isAcpRuntime(id)))`
- 映射为动态 `TermTemplate`（`id: dyn-${rt.id}`, `runtime: rt.id`）
- 与已存 template 按 runtime id 去重
- 独立分区 **「ACP Agents」**；点击 `spawnTerminal(project, t)` → `spawnAgent(proj, "acp:opencode")`
- `spawnAgent` ACP 支：`addAgent(..., null)` + `markAcpLive`（承 Phase2 首轮）
- title 标明「结构化面板，非终端」；badge `ACP`
- 第二个 ACP agent：仅 descriptor + available 即可进菜单（延伸 DoD 代码路径满足）

**主 UX：** 项目 `+` / TabBar `+` → Picker → OpenCode (ACP)。  
非挡板：Composer 空发仍默认 claude（Dev 明示；F1 以 Picker 为准）。  
Ctrl+T Settings 仍可旁路选 ACP（加分，非必须）。

### B spawnTerminal — PASS
```ts
if (template.command && template.runtime.startsWith("bash")) { agent_write... }
```
ACP `command:""` → 不写 PTY。

### C 侧栏 switch — PASS
```ts
runtimes.filter((rt) => !isAcpRuntime(rt.id) && rt.transport !== "acp")
```
注释指向 Picker 创建。不会对 ACP 调 `agent_switch_runtime`。

### D DEF-007 — PASS（closed）
`AcpBridge::prompt`：
1. emit `Status { running }` **先于** `handle.prompt`
2. `result.is_err()` → emit `idle`
3. 成功路径仍靠 host `turn_done` / stopReason → idle（既有）
4. `prompt_async`（`acp_prompt` 命令）调用 `bridge.prompt` → 同路径

FE：`status === "busy" \|\| "running"` → `turnActive`；Stop 不再只靠乐观 busy。

### E panel/composer 回归 — PASS（无回归 diff）
本轮未改 Composer/ContentArea/AcpSessionPanel；承 `1088ba1cc` 已证：
- isAcp 纪律、acp_prompt/cancel、方言隐藏、事件 flatten+agentId

### F 回归 — PASS
```text
pnpm exec tsc --noEmit     → 0
cd src-tauri && cargo test acp → 9 passed
```

### G LeftSidebar 范围 — PASS
- commit 仅 ACP filter + import
- `git status`：LeftSidebar 仍 `M`（用户脏改），Test **未覆盖**

---

## 3. Defects

| id | status |
| --- | --- |
| DEF-006 | **closed**（F1 Picker 一等入口） |
| DEF-007 | **closed**（prompt→running） |
| DEF-001/002 | closed |
| DEF-005 | partial → Phase3 permission |

无新开 DEF。

### 观察（不挡 Phase2）
- UI 真机 E2E（点 Picker→流式→Stop）本环境 Wayland 未自动化；**代码路径 + CLI 门禁**满足编排对 Test 的约束。全量 §12 UI 签字可在有显示自动化或人工点验时补。
- Composer 默认 spawn 仍 claude — 可接受。

---

## 4. Phase 2 出口判定

| 硬门槛 | 结果 |
| --- | --- |
| F1 主路径可发现/spawn `acp:opencode` | **PASS** |
| isAcp / 无 ACP agent_write / 隐藏 PTY 方言 | PASS |
| AcpSessionPanel + 事件隔离 | PASS（代码） |
| Stop / running 状态 | PASS（FE+Host） |
| tsc / cargo acp | PASS |
| LeftSidebar 用户改动未吞 | PASS |

**phase_gate = test_passed**  
**current_phase 仍为 2**（+phase→3 由 Watcher）

Phase3 仍：permission Host 策略、fs 沙箱、真工具 F5–F9。

---

## 5. 给 Dev / Watcher

- **Watcher：** `test_passed` → `current_phase=3`，`phase_gate` 置位待 Dev Phase3。
- **Dev：** 可开 **Phase 3**（permission 闭环 + fs/read 沙箱 + OpenCode 真工具）。完成后 `dev_done` + 直接 send Test。
- UI 实跑若后续有条件，优先：Picker→AcpSessionPanel→prompt 流式→Stop cancel→kill；多 tab 不串。

---

## 6. 命令索引

```bash
git show 243c97de1 --stat
pnpm exec tsc --noEmit
cd src-tauri && cargo test acp
```
