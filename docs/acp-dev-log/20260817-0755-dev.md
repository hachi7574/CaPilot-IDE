# ACP Dev Log — 2026-08-17T07:55 (Phase 3 permission + fs sandbox)

role: dev  
agentId: ad172813-30c1-4bea-82fe-ddfa1e4b0885  
phase: 3  
gate_out: dev_done

## Summary
Phase 3：permission 默认 ask 闭环（mock allow/reject 集成测绿）；`fs/read_text_file` cwd 沙箱 + write 硬拒；security-review §6；DEF-005 mock permission 单调 id 修复。

## Key files
| path | change |
| --- | --- |
| `src-tauri/src/agent_runtime/acp/fs_sandbox.rs` | **new** resolve_under_root / read_text_file / write disabled + 6 unit tests |
| `src-tauri/src/agent_runtime/acp/host.rs` | handle fs read/write; unknown method -32601; cwd through pump/handle_line |
| `src-tauri/src/agent_runtime/acp/bridge.rs` | respond_permission → status running; permission+fs integration tests |
| `src-tauri/src/agent_runtime/acp/mod.rs` | mod fs_sandbox |
| `src-tauri/tests/fixtures/mock_acp_agent.py` | monotonic req ids; fsread:; permission outcome→tool status |
| `docs/security-review.md` | §6 ACP threat surface |

## Policy (MVP)
- permission: **ask only** (no auto/yolo)
- fs write: **off** (capabilities + host error)
- fs read: absolute + under session cwd after canonicalize; symlink escape denied; 2 MiB cap
- terminal elicitation: off

## Verification
```
cd src-tauri && cargo test acp   # 18 passed
pnpm tsc --noEmit               # 0
opencode acp bootstrap          # initialize+session/new OK (no paid prompt)
```

## Notes for Test
- OpenCode may run tools in-process without client `request_permission` / `fs/*` (Phase0). Mock path proves Host when agent does call client methods.
- Wayland: no UI click E2E this round.
- LeftSidebar user dirt untouched.

## Handoff
Direct send Test `d62ccd50-e114-4625-a058-6bb75a76dad6`.
