# Test Report — 2026-08-17 ~08:10 (+08)

agentId: `d62ccd50-e114-4625-a058-6bb75a76dad6`  
role: Test  
trigger: Dev Phase3 `dev_done` / commit `65b851cf9` / log `20260817-0755-dev.md`  
current_phase: **3** → **test_passed**

## 0. 实际 diff

```text
65b851cf9 feat(acp): phase 3 — permission ask policy, fs sandbox, security notes
```

| 路径 | 结论 |
| --- | --- |
| `acp/fs_sandbox.rs` | **new** resolve_under_root / read / write 硬拒 + 6 unit |
| `acp/host.rs` | fs read/write 应答；unknown method -32601；permission ask-only；cwd 贯穿 |
| `acp/bridge.rs` | respond_permission → status running；集成测 allow/reject + fs in/out |
| `acp/mod.rs` | `mod fs_sandbox` |
| `mock_acp_agent.py` | 单调 agent req id（DEF-005）；`fsread:`；permission→tool status |
| `docs/security-review.md` | §6 ACP 威胁面 |
| `docs/acp-dev-*` | status + dev log |
| `LeftSidebar.tsx` | **不在 commit**；WT 用户脏改保留 |

---

## 1. 本轮计划（按 diff 自定）

| # | 角度 | 做法 |
| --- | --- | --- |
| A | permission 状态机 | Host ask-only；emit PermissionRequest+waiting_input；respond allow/reject；status→running；无 auto/yolo |
| B | FE permission 闭环 | AcpSessionPanel → `acp_respond_permission`；store `permission_request`→pending + Tab 待确认 |
| C | fs 沙箱 | 绝对路径 / 出界 / symlink / write off / 2MiB / line slice；Host 错误码 |
| D | capabilities | initialize `writeTextFile:false` `terminal:false`；write 仍硬拒 -32003 |
| E | mock DEF-005 | 单调 id；allow→completed / reject→failed；fsread in/out |
| F | security §6 | 威胁表与实现一致；OpenCode 进程内工具 residual 写明 |
| G | 回归门禁 | `cargo test acp` + tsc；LeftSidebar 未吞 |
| H | F5–F9 OpenCode 真 UI | Wayland 豁免；进程内工具 Phase0 已知 |

---

## 2. 结果

### A permission Host — PASS
- `session/request_permission`：**从不**自动 allow；只 insert board + emit `PermissionRequest` + `Status{waiting_input}` + 内部 `WaitingPermission`
- `respond_permission`：Allow/Reject/Cancelled 映射 outcome；若 status 为 WaitingPermission → Running
- Bridge：`respond_permission` 后额外 emit `status: running`（离开 waiting_input UX）
- 无 auto/yolo 代码路径（policy 注释 + security §6 residual）

### B FE 接线 — PASS（代码路径）
- `AcpSessionPanel.respond` → `invoke("acp_respond_permission", { id, requestId, outcome, optionId })`
- `applyAcpEvent` `permission_request` → `pendingPermission` + permission item + `AgentInfo.status=waiting_input`
- `status` busy/running → `turnActive` + agent running；perm card 显隐绑 `pendingPermission`
- lib 命令：allow / reject / else Cancelled

### C fs 沙箱 — PASS
Unit（`fs_sandbox`）：
- relative → `NotAbsolute`
- under root read OK
- outside → `OutsideRoot`
- symlink escape → `OutsideRoot`（unix）
- write always `WriteDisabled`
- line/limit slice `Some(2),Some(2)` → `b\nc`

Host：
- `fs/read_text_file` → sandbox；出界/相对 `-32001`；not found `-32002`；过大 `-32004`；cwd 空 `-32000`
- `fs/write_text_file` → 一律 `-32003`（不看 path）
- unknown agent method + id → `-32601`

Integration：`fs_read_under_cwd_ok_and_outside_denied` — `fs_ok:…sandbox-ok` / `fs_err:…escapes`

### D capabilities — PASS
```json
"clientCapabilities": {
  "fs": { "readTextFile": true, "writeTextFile": false },
  "terminal": false
}
```
与 write 硬拒 / terminal 不实现双保险一致。

### E mock DEF-005 — PASS（closed）
- `next_agent_req` 自 9000 单调递增（permission + fsread）
- allow optionId 含 `allow` → tool `completed`；reject → `failed`
- 集成测 `permission_request_allow_roundtrip` / `permission_request_reject_roundtrip` 绿

### F security-review §6 — PASS
表覆盖：IPC 命令、permission ask、fs read/write、terminal off、env 不注入 TUI config、PTY/ACP 分叉。  
Residual：OpenCode 进程内工具不回调 client — 与 Phase0/Dev 交接一致，**不挡** Host 策略闭环。

### G 回归 — PASS
```text
cd src-tauri && cargo test acp  → 18 passed; 0 failed
pnpm exec tsc --noEmit          → 0
```
LeftSidebar：commit 无该文件；`git status` 仍 `M` 用户脏改，Test **未覆盖**。

### H F5–F9 OpenCode 真 UI — EXEMPT（记豁免，不 fail）
- Wayland：无可靠 UI 点击自动化（既有约束）
- OpenCode 可能进程内跑工具，不触发 client `request_permission` / `fs/*`（Phase0）
- Host+mock 已证策略在 agent **确实**回调时生效
- §12：F5–F8 **mock 集成路径** PASS；F5–F9 OpenCode 真 UI 标豁免/代码路径，待有显示自动化或人工点验再补签字

---

## 3. Defects

| id | status |
| --- | --- |
| DEF-005 | **closed**（单调 req id + allow/reject 集成绿） |
| DEF-001/002/006/007 | closed（承前） |

**无新开 DEF。**

### 观察（不挡 Phase3）
- FE respond 后本地清 `pendingPermission` + 乐观 busy；Host 亦 emit running — 双路径可接受。
- `starts_with` 边界：cwd 为 `/foo` 时 `/foo-bar` 理论上可误判为 under-root；Linux 绝对路径 + canonicalize 实战 cwd 多为真实目录；可 Phase4 hardening（非 blocker）。
- OpenCode 真工具 permission/fs 仍依赖 agent 是否走 client methods。

---

## 4. Phase 3 出口判定

| 硬门槛 | 结果 |
| --- | --- |
| permission 默认 ask 闭环（emit + respond + status） | **PASS**（Host+mock+FE 代码） |
| fs/read 出界拒绝 + symlink | **PASS** |
| fs/write 禁用（capability + Host） | **PASS** |
| DEF-005 mock 稳 | **PASS / closed** |
| security-review §6 | **PASS** |
| cargo acp 18 / tsc 0 | **PASS** |
| LeftSidebar 用户改动未吞 | **PASS** |
| F5–F9 OpenCode 真 UI | **EXEMPT**（Wayland + in-process tools） |

**phase_gate = test_passed**  
**current_phase 仍为 3**（+phase→4 由 Watcher）

---

## 5. 给 Dev / Watcher

- **Watcher：** `test_passed` → 可 `current_phase=4`，`phase_gate` 置位待 Dev Phase4。
- **Dev：** 可开 **Phase 4**（Settings/resume/usage、§12 总表、RUNBOOK / ai-runtime-references 链接、附录 A）。**勿**在未推进 phase 前混做 Phase4 大改。
- 真机有条件时优先补：permission 卡 Allow/Reject 点验；若 OpenCode 不回调则文档化豁免即可。

---

## 6. 命令索引

```bash
git show 65b851cf9 --stat
cd src-tauri && cargo test acp
pnpm exec tsc --noEmit
```
