# ACP Dev Log — 2026-08-17T09:15 (Phase 4 productization)

role: dev  
agentId: ad172813-30c1-4bea-82fe-ddfa1e4b0885  
phase: 4  
gate_out: dev_done

## Summary
Phase 4：Settings ACP Agents CRUD（list/upsert/remove + 启停）；`usage_update` → `AgentInfo.last_usage`；resume `session/load` mock 集成测；RUNBOOK / ai-runtime-references 链接；附录 A.4 签字。

## Key files
| path | change |
| --- | --- |
| `src-tauri/src/agent_runtime/acp/descriptor.rs` | validate / upsert / remove / list_for_settings + unit tests |
| `src-tauri/src/lib.rs` | `acp_list_agents` / `acp_upsert_agent` / `acp_remove_agent` |
| `src-tauri/src/agent_runtime/acp/bridge.rs` | `bridge_resume_via_session_load`（F11 mock） |
| `ui/components/layout/SettingsModal.tsx` | 「ACP」分区 CRUD UI |
| `ui/state/store.ts` | usage → last_usage 镜像（D9） |
| `docs/CaPilot-IDE-RUNBOOK.md` | §5 ACP 索引 |
| `docs/ai-runtime-references.md` | §1 ACP 行 + §2.3a 双轨边界 |
| `docs/acp-runtime-plan.md` | 附录 A.4 验收记录 |

## Policy (unchanged MVP)
- permission: **ask only**
- fs write: **off**
- fs read: under session cwd
- PTY adapters untouched；LeftSidebar 用户 dirt 未动

## Verification
```
cd src-tauri && cargo test acp   # 20 passed
pnpm tsc --noEmit               # 0
```

## Notes for Test
- Settings → ACP：列表 / 添加 / 编辑 / 禁用 / 删除（内置 default 删除 = enabled:false 阴影）。
- resume：mock `loadSession` + resume_key → sessionId 采用；真 OpenCode 见附录 A.2。
- usage：agent 发 `usage_update` 时面板 + 共享 meter。
- Wayland：无 UI 点击 E2E；代码走读 + cargo/tsc。
- 勿混 `opencode`（PTY）与 `acp:opencode`。

## Handoff
Direct send Test `d62ccd50-e114-4625-a058-6bb75a76dad6`.
