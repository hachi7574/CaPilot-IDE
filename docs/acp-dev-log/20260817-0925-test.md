# Test Report — 2026-08-17 ~09:25 (+08)

agentId: `d62ccd50-e114-4625-a058-6bb75a76dad6`  
role: Test  
trigger: Dev Phase4 `dev_done` / commit `e7a894249` / log `20260817-0915-dev.md`  
current_phase: **4** → **test_passed**；**goal_met=true**（代码/mock 门禁 + 既有 Wayland 豁免）

## 0. 实际 diff

```text
e7a894249 feat(acp): phase 4 — Settings CRUD, resume test, usage mirror, docs
```

| 路径 | 结论 |
| --- | --- |
| `acp/descriptor.rs` | validate / upsert / remove（default→enabled:false 阴影）/ list_for_settings + `validate_rejects_bad_id` |
| `lib.rs` | `acp_list_agents` / `acp_upsert_agent` / `acp_remove_agent` 注册 |
| `acp/bridge.rs` | `bridge_resume_via_session_load`（F11 mock） |
| `SettingsModal.tsx` | 侧栏「ACP」+ 列表/添加/编辑/启停/删除 |
| `store.ts` | `usage` → `AgentInfo.last_usage`（D9） |
| RUNBOOK / ai-runtime-references / 附录 A.4 | 链接与签字 |
| LeftSidebar / PTY adapters | **不在 commit** |

---

## 1. 本轮计划

| # | 角度 | 做法 |
| --- | --- | --- |
| A | F12 Settings CRUD 后端 | validate slug；upsert 用户文件；remove 阴影 default；list 含 disabled + available |
| B | F12 Settings UI | 分区/表单字段/serde 形状；save→reDetect；id 仅新建可填 |
| C | F11 resume | host loadSession+resume_key→session/load；mock 集成测 sessionId |
| D | D9 usage | applyAcpEvent usage→last_usage 字段映射 |
| E | 文档 | RUNBOOK §5.1；references §1+§2.3a；附录 A.4 |
| F | 回归 | cargo acp ≥20；lib 全量；tsc；LeftSidebar/PTY 未动 |
| G | DoD / goal_met | 勾选与豁免一致性 |

---

## 2. 结果

### A descriptor CRUD — PASS
- `validate_descriptor`：拒 `acp:` / `/` / `\` / 空格；command/name 必填；单测 `validate_rejects_bad_id`
- `upsert_user_descriptor` → `~/CaPilot/acp-agents.json`（trim id/name/command）
- `remove_user_descriptor`：用户项删除；**内置 default** 写 `enabled:false` 阴影（command 空）
- `list_for_settings`：defaults∪user（含 disabled）；`available = enabled && command_on_PATH`；`runtime_id=acp:{id}`；`is_default`
- 命令：`acp_list_agents` / `acp_upsert_agent` / `acp_remove_agent` 已 invoke 注册

### B Settings UI — PASS（代码路径）
- 导航「ACP / 协议 Agent」+ `settings-acp` 分区；文案区分 PTY `opencode` vs `acp:opencode`
- 列表：runtimeId、default 徽标、可用/禁用/命令未找到、ON/OFF、编辑、删除（default 删除 title 标明阴影）
- 新建：id 短 slug + name + command + args；保存 `acp_upsert_agent` 后 `loadAcpAgents` + `reDetect`（`runtime_list_available`）
- 编辑：不改 id；name/command/args
- Icon `plug` 资源存在
- **serde：** `AcpAgentListItem` outer `camelCase`（`runtimeId`/`isDefault`）+ flatten descriptor **snake**（`cwd_mode`）；FE 接口与 upsert payload 一致（`cwd_mode`）

### C F11 resume — PASS（mock）
- Host：`agentCapabilities.loadSession` + `resume_key` → `session/load`；否则 `session/new`；sessionId 采用结果或 fallback resume_key
- mock 宣称 `loadSession: true`；`bridge_resume_via_session_load`：`resume_key=sess_resumed_42` → `acp_session_id` 匹配后可 prompt
- OpenCode 真 resume：附录 A / Dev 说明能力依赖 agent；不挡 mock 路径

### D D9 usage — PASS
- `usage` 事件规范化 `Number(used/size)`
- `size > 0` 时镜像 `last_usage.contextWindowUsedTokens/MaxTokens`（cache/model null）
- 面板既有 `session.usage` 显示（承前）

### E 文档 — PASS
- RUNBOOK：文档地图 + §5.1 ACP 双轨速览（descriptor/UI/安全/plan 链）
- ai-runtime-references：§1 ACP 行；§2.3a PTY vs ACP 表 + CRUD/usage/安全
- 附录 A.4：2026-08-17 签字行（代码/mock PASS；F5–F9 EXEMPT）

### F 回归 — PASS
```text
cd src-tauri && cargo test acp     → 20 passed
cd src-tauri && cargo test --lib   → 226 passed; 0 failed; 1 ignored
pnpm exec tsc --noEmit             → 0
```
- LeftSidebar：**不在** `e7a894249`；WT 用户 `M` 保留  
- PTY adapters：**无** runtime adapter 文件变更

### G DoD / goal_met
| DoD | 判定 |
| --- | --- |
| 锚点闭环 spawn→prompt→流式→perm→cancel→kill | **代码/mock 路径 PASS**；OpenCode 真 UI 点验承 Phase3 **EXEMPT**（Wayland + 进程内工具） |
| §12 无未豁免失败 | F5–F9 EXEMPT 已记；F11 mock / F12 代码 / D9 代码 PASS |
| 显示 / 输入区纪律 | 承 Phase2–3 代码路径；本轮无回归 diff |
| 双轨无损 | lib **226**；PTY 未迁 |
| 安全 MVP | 承 Phase3 |
| tsc / 文档 / 第二 agent descriptor | PASS（Settings CRUD + Picker 动态 ACP） |

**goal_met = true**（门禁口径与 Phase2/3 一致：代码+mock+bootstrap+文档；真 UI E2E 非本环境硬门槛）。

---

## 3. Defects

无新开 DEF。既有 DEF 全 closed。

### 观察（不挡）
1. **删除内置 default 阴影** `command:""`：列表仍显示 disabled；直接 ON 会因 validate 失败——需编辑补全 command/args。可接受 MVP；后续可阴影保留原 command。
2. Settings 编辑不暴露 env/cwd_mode/icon（写死 session / 空 env）— MVP 足够。
3. 锚点 **真机全链路 UI** 仍未自动化点验；有显示条件时可补人工签字，不推翻 goal_met。

---

## 4. Phase 4 出口判定

| 硬门槛 | 结果 |
| --- | --- |
| F12 Settings CRUD 后端+UI | **PASS** |
| F11 resume mock | **PASS** |
| D9 usage→last_usage | **PASS** |
| 文档链接 + A.4 | **PASS** |
| cargo acp 20 / lib 226 / tsc 0 | **PASS** |
| LeftSidebar / PTY 未吞 | **PASS** |

**phase_gate = test_passed**  
**current_phase = 4**（末相，不 +phase）  
**goal_met = true**

---

## 5. 给 Dev

- Phase 4 **关闭**；总目标在既定豁免政策下 **goal_met**。
- 可选后续（非门禁）：真机 UI 点验补签；default 阴影保留 command；env 编辑。
- **未** notify Watcher（拓扑禁止 Test→Watcher）。

---

## 6. 命令索引

```bash
git show e7a894249 --stat
cd src-tauri && cargo test acp && cargo test --lib
pnpm exec tsc --noEmit
```
