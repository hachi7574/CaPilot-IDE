# 2026-08-17 11:20 Dev — Phase5 产品可用性（假 goal_met 撤销后）

## 背景
用户真机：`hi` → session/prompt Rate limit；Composer 无模型/思考按钮。  
原 `goal_met=true` 仅 eng/mock 门禁，**已撤销**。

## 改动
- `host.rs`：`preferred_model` bootstrap → `session/set_config_option`；`pick_bootstrap_model` 优先 zen free；`humanize_acp_error`；prompt rate-limit fallback 链（go deepseek-v4-flash 等）；`set_model`/`last_model`
- `bridge.rs`：传 preferred model；`set_model`/`set_config_option`/`note_model`；smoke `opencode_acp_real_prompt_smoke`
- `registry.rs`：OpenCode ACP 模型目录（Zen free 默认）+ effort
- `lib.rs`：`agent_set_session_config` live ACP apply；spawn 持久化 effective model
- `Composer.tsx`：ACP 显示模型/思考菜单；thinking 走 set_session_config
- `agentActions.ts`：ACP spawn pin 默认/选中模型
- `store.ts` / `acpTypes.ts`：`session_started.model` 写回 AgentInfo
- `mock_acp_agent.py`：`session/set_config_option`

## 自测
```
cd src-tauri && cargo test acp
cd src-tauri && cargo test --lib opencode_acp_real_prompt_smoke -- --nocapture
# → ok, model=opencode-go/deepseek-v4-flash
pnpm tsc --noEmit  # 0
# 手工 NDJSON：zen free set OK 但 prompt rate limit；go flash prompt pong OK
```

## limits
- Zen free 本机 Console 仍限流（DEF-010）
- 未做 CaPilot 窗口内点击（Wayland）
- 思考强度依赖模型是否暴露 effort options

## 未碰
- `ui/components/layout/LeftSidebar.tsx`
